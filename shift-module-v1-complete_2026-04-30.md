# 倒班排班模块 — V1 实现完成

**时间：** 2026-04-30 18:35 GMT+8  
**状态：** ✅ 全部文件写入完成，待真机/模拟器验证

---

## 新增文件清单（12 个文件）

### 核心引擎
| 文件 | 说明 |
|------|------|
| `src/utils/shift-engine.js` | 排班规则引擎：日期→班次推算、倒计时、月度统计、月历数据生成、5套预设模板 |

### 排班首页 (Tab2)
| 文件 | 说明 |
|------|------|
| `pages/shift/home/home.js` | 逻辑：读取活跃规则、今日班次、倒计时、近7天、本月统计 |
| `pages/shift/home/home.wxml` | 模板：大色块今日卡片 + 倒计时 + 横向滑动7天 + 月度统计 + 快捷入口 |
| `pages/shift/home/home.wxss` | 样式：各色班次背景、绿色点缀、白色卡片 |
| `pages/shift/home/home.json` | 导航标题「排班」 |

### 排班日历
| 文件 | 说明 |
|------|------|
| `pages/shift/calendar/calendar.js` | 逻辑：月历渲染、点击查看、换班弹窗、清除覆盖、记忆月份位置 |
| `pages/shift/calendar/calendar.wxml` | 模板：月历网格（带班次色标签）+ 选中日期信息卡 + 换班弹窗 |
| `pages/shift/calendar/calendar.wxss` | 样式：网格布局、色块标签、底部弹窗 |

### 排班设置
| 文件 | 说明 |
|------|------|
| `pages/shift/settings/settings.js` | 逻辑：预设选择、确认/编辑、自定义规则、日期选择、序列编辑、颜色/时间修改、保存/删除 |
| `pages/shift/settings/settings.wxml` | 模板：模板卡片列表 + 编辑面板（日期/序列/班次明细），含 wxs 辅助函数 |
| `pages/shift/settings/settings.wxss` | 样式：模板卡片选中态、表单样式、色块预览 |

### 修改文件
| 文件 | 改动 |
|------|------|
| `src/app.json` | 注册3个新页面 + 添加底部 tabBar（日程 \| 排班） |

---

## 功能覆盖

### ✅ 已实现
- 5 套预设模板（四班三运转、三班两倒、四班两倒、上二休二、五班三运转）
- 核心推算：起始日 + 序列 + 目标日期 = 唯一班次
- 今日大卡片（班次名称 + 时间 + 颜色背景）
- 倒计时（距下次上班天数）
- 近 7 天横向滑动预览
- 本月班次统计（各类型天数）
- 月历视图（每个日期格显示班次色条）
- 换班调整（选中日期 → 选目标班次覆盖）
- 清除覆盖恢复自动推算
- 自定义规则（序列、颜色、时间自由编辑）
- 规则持久化存储（wx.setStorageSync）
- TabBar 双标签导航

### ⚠️ 待验证/调整
- 底部 tabBar 在微信开发者工具中的表现（无自定义图标时纯文字显示）
- 日历页与首页之间的 switchTab 跳转
- 设置页 navigateBack 的层级关系
- list/list 页（日程）的 onShow 刷新逻辑与 tabBar 的兼容性
